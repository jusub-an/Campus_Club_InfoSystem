<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.zerock.mapper.ApplicationMapper">
	<!-- 동아리 가입 신청 SQL -->
	<select id="getApplyList" resultType="org.zerock.domain.ApplicationDTO">
		select * from Application where club_id = #{club_id}
	</select>
    
	<insert id="insert">
		INSERT INTO Application (club_id, applicant_email, status, applied_at)
		VALUES (#{club_id}, #{applicant_email}, #{status}, #{applied_at})
	</insert>
	
	<select id="getMemberEmailByClubId" resultType="java.lang.String">
		select user_email from Member where user_email = #{user_email} and club_id = #{club_id}
	</select>
	
	<select id="getApplyEmailByClubId" resultType="java.lang.String">
		select applicant_email from Application where applicant_email = #{applicant_email} and club_id = #{club_id}
	</select>
	
	<select id="getName" resultType="java.lang.String">
		select name from Users where user_email = #{applicant_email}
	</select>
	
	<select id="countByApplicantEmailAndClubId" resultType="int">
 		SELECT COUNT(*) FROM Application WHERE applicant_email = #{applicant_email} and club_id = #{club_id}
 	</select>
 	
	<delete id="delete">
        DELETE FROM Application WHERE app_id = #{app_id}
    </delete>
    
	<!-- 동아리 회원 SQL -->
	<select id="getMemberList" resultType="org.zerock.domain.MemberDTO">
		select * from Member where club_id = #{club_id}
	</select>
	
    <insert id="insertMember">
        INSERT INTO Member (club_id, user_email)
        VALUES (#{club_id}, #{user_email}) 
    </insert>
    
    <delete id="deleteMember">
        DELETE FROM Member WHERE club_id = #{club_id} AND user_email = #{user_email}
    </delete>
</mapper>