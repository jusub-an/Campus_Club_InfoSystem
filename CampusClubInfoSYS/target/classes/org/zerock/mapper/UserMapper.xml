<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.zerock.mapper.UserMapper">

    <!-- 회원가입 -->
    <insert id="register">
        INSERT INTO users (user_email, password, name, student_id, user_type_code)
        VALUES (#{user_email}, #{password}, #{name}, #{student_id}, #{user_type_code})
    </insert>

    <!-- 이메일로 사용자 정보 조회 (로그인 시 사용) -->
    <!-- "User" 테이블 이름이 대소문자를 구분할 수 있으므로 큰따옴표로 감쌌습니다. -->
    <select id="read" resultType="org.zerock.domain.UserDTO">
        SELECT * FROM users WHERE user_email = #{user_email}
    </select>
    
    <!-- 이메일 중복 체크 -->
    <select id="checkEmail" resultType="int">
        SELECT COUNT(*) FROM users WHERE user_email = #{user_email}
    </select>
    
    <select id="findIdByNameAndStudentId" resultType="string">
        SELECT user_email 
        FROM users 
        WHERE name = #{name} AND student_id = #{student_id}
    </select>

    <select id="findUserForPwReset" resultType="org.zerock.domain.UserDTO">
        SELECT * FROM users 
        WHERE user_email = #{user_email} AND name = #{name} AND student_id = #{student_id}
    </select>
    
    <update id="updatePassword">
        UPDATE users
        SET password = #{password}
        WHERE user_email = #{user_email}
    </update>

</mapper>